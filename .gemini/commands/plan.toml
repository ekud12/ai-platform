description = "Plan → Approve → Code → Review → Fix → Complete loop"

prompt = """
# PLAN & EXECUTE LOOP

## REQUEST
{{args}}

## EXECUTION LOOP

### 1. PLAN
Use the **planner** skill to analyze the request and create an execution plan.

### 2. APPROVE
**STOP AND ASK THE USER:**
"Here is my plan: [summary]. Do you approve? (yes/no/modify)"

- If **no** → ask what to change, go back to step 1
- If **modify** → adjust plan, show again
- If **yes** → proceed to step 3

### 3. CODE
Use **coder-dotnet** or **coder-typescript** skill based on the code type.

### 4. REVIEW
Use **reviewer-dotnet** or **reviewer-typescript** skill to check against all rules.

### 5. DECISION
- If ANY critical/major violations → go to step 6
- If PASS → go to step 7

### 6. FIX
Fix the violations. Go back to step 4 (max 5 iterations).

### 7. COMPLETE
```json
{
  "success": true,
  "files": ["..."],
  "iterations": N
}
```

## BEGIN
Start with PLAN.
"""
