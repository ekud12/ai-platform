description = "Review TypeScript code changes using the TS Reviewer Agent"
prompt = """
@../../agents/roles/reviewer.typescript.agent.md

## Code Changes to Review
The user has prepared the following changes:

!{git diff --staged}

## User Input (Additional Context)
{{args}}
"""
